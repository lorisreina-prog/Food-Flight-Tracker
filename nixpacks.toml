[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["python -m pip install -r FFT-backend/requirements.txt"]

[start]
cmd = "cd FFT-backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
